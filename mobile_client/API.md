API detail book thêm 1 trường isRead nếu = 0 thì sẽ trả về sách chưa mua, nếu = 1 thì sẽ trả về sách đã mua
API detail book thêm 1 trường lưu tiến trình sách đang đọc đến chapter bao nhiêu và nghe đang đến chapter bao nhiêu theo API post đề cập bên dưới, nếu không có maặc định là chapter đầu


Cần thêm 1 API Post để chuyển đổi trạng thái bookmark(favourite)


Cần 1 API Post tiến trình đang đọc của ebook, khi đọc xong 1 chapter thì sẽ gửi lên server để lưu lại tiến trình đọc của người dùng, khi mở lại sách đó thì sẽ load lại tiến trình đọc của người dùng tương tự với audioChapter